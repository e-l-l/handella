/**
 * The class strings more than one component spells. Kept here rather than
 * beside any one of them, so restyling a control is one edit instead of a
 * search: the form field and the primary button are each worn by several
 * pages, and three copies of a Tailwind string is three chances to drift.
 */

export const fieldClass =
  'w-full rounded-xl border border-line bg-surface px-3 py-2 text-sm text-ink'

export const primaryButtonClass =
  'rounded-full bg-brand px-4 py-2 text-sm font-semibold text-white'
